# Webhook del agente (n8n / backend). Cuando se edita un producto,
# se hace POST a este endpoint para sincronizar la memoria del agente.
AGENT_SYNC_WEBHOOK=

# Token opcional para autenticar el webhook
AGENT_SYNC_TOKEN=
